// complete as variaveis necessarias
var n = parseInt();

while ( > 0) { //complete o codigo
  var c = parseFloat();
  let dias = ; //atribua o valor correto a variavel
  while (   ) { // complete o while
    // complete o codigo
  }
  console.log(`  dias`); //complete o código para a saída
}
